# GraphQL Configuration - DEVELOPMENT
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:3007/graphql
NEXT_PUBLIC_GRAPHQL_ENDPOINT=http://localhost:3007/graphql

# Backend Configuration (CRITICAL for WebSocket chat) - DEVELOPMENT
NEXT_PUBLIC_BACKEND_URL=http://localhost:3007

# WebSocket Configuration (Socket.IO for chat/signaling) - DEVELOPMENT
NEXT_PUBLIC_WS_URL=ws://localhost:3007/signaling
NEXT_PUBLIC_SIGNALING_WS=ws://localhost:3007/signaling

# LiveKit Configuration (WebRTC for video) - DEVELOPMENT
NEXT_PUBLIC_LIVEKIT_URL=ws://localhost:7880

# Application Configuration - DEVELOPMENT
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Environment
NODE_ENV=development

